image: node:16

stages:
  - build
  - test
  - deploy staging
  - deploy production
  - production tests

variables:
  STAGING_DOMAIN: next-staging.surge.sh
  PRODUCTION_DOMAIN: next-basket.surge.sh

cache:
  key: $CI_COMMIT_SHORT_SHA
  paths:
    - node_modules/

build website:
  stage: build
  script:
    - echo $CI_COMMIT_SHORT_SHA
    - npm install
    - npm i @angular/cli
    - npm run build
    - sed -i "s/%%VERSION%%/$CI_COMMIT_SHORT_SHA/" ./src/index.html
    - ls

  artifacts:
    paths:
      - ./src
test artifact:
  image: alpine
  stage: test
  script:
    - grep -q "Tabaka" ./src/index.html

test website:
  stage: test
  script:
    - npm install
    - npm i @angular/cli
    - npm run start &
    - sleep 30
    - curl "http://localhost:4200/" | tac | tac | grep -q "Tabaka"

deploy staging:
  stage: deploy staging
  environment:
    name: staging
    url: HTTP://$STAGING_DOMAIN
  script:
    - npm install --global surge
    - surge --project ./src --domain next-staging.surge.sh

deploy production:
  stage: deploy production
  environment:
    name: staging
    url: http://$PRODUCTION_DOMAIN
  script:
    - npm install --global surge
    - ls
    - surge --project ./src --domain next-basket.surge.sh

production tests:
  image: alpine
  stage: production tests
  script:
    - apk add --no-cache curl
    - curl -s "https://next-basket.surge.sh/" | grep -q "Tabaka"
    - curl -s "https://next-basket.surge.sh/" | grep -q "$CI_COMMIT_SHORT_SHA"
