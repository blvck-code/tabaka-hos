image: node:16

stages:
  - build
  - test
  - deploy

build website:
  stage: build
  script:
    - npm install
    - npm i @angular/cli
    - npm run build

  artifacts:
    paths:
      - ./src
test artifact:
  image: alpine
  stage: test
  script:
    - grep -q "Tabaka" ./src/index.html

test website:
  stage: test
  script:
    - npm install
    - npm i @angular/cli
    - npm run start &
    - sleep 30
    - curl "http://localhost:4200/" | tac | tac | grep -q "Tabaka"


deploy to surge:
  stage: deploy
  script:
    - npm install --global surge
    - surge --project ./src --domain next-basket.surge.sh

